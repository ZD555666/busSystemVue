<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.wx.wxBus.mapper.ComplaintMapper">

    <insert id="insertComplaint">
        insert into
        t_wx_complaint
        (phoneNumber,problem,imagePath)
        values(#{phoneNumber},#{problem},#{imagePath});
    </insert>

</mapper>
